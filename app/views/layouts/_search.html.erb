<div class="search-form">
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({ "投稿" => "post", "コラム" => "column" }) %>
    <%= f.submit '検索', class: "btn btn-info" %>
  <% end %>
</div>
<div class="container">
　<div class="row">
    <div class="col-2"></div>
	　<div class="col-8">
	    <h1 class="mt-5 text-center"><%= @post.user.profile_image %><%= @post.user.name %>さんの釣果</h1>
	    <table class="table">
	    	<tr><th>写真</th></tr>
	    	<tr><td><%= attachment_image_tag @post, :image, class: "fish-image" %></td></tr>
	    	<tr><th>釣った魚</th></tr>
	    	<tr><td><%= @post.name %></td></tr>
	    	<tr><th>説明</th></tr>
	    	<tr><td><%= @post.body %></td></tr>

	    	<tr><td><%= @post.created_at %></td></tr>
	    </table>
    </div>
    <div class="col-2"></div>
  </div>
</div>